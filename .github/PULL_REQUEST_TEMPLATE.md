<!--
Adding a result file under results/<your-handle>/? Everything below except the first line is optional —
the check will comment with a link that finishes the submission for you.
-->

## What this changes

<!-- One or two sentences. For a catalog entry, the part and where its specs came from. -->

## Checks

- [ ] `npm --prefix frontend run typecheck` passes
- [ ] `npm --prefix frontend run catalog:validate` passes, if I touched the catalog
- [ ] UI copy reads like the rest of the site, if I changed what people see
