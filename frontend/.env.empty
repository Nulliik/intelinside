# Loaded by `vite --mode empty` (the frontend-empty launch config): the site with nobody on it yet. Every board,
# rig list, and detail page shows its empty state. Sign in as any seeded user to try the flows from scratch.
VITE_MOCK_EMPTY=true
