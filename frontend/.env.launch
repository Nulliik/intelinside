# Loaded by `vite --mode launch` (the frontend-launch launch config): launch morning. The board is sealed and
# nobody has registered a rig or posted a result yet. Sign in as any seeded user to try the empty flows.
VITE_REVEAL_AT=2026-09-11T16:00:00Z
VITE_MOCK_EMPTY=true
