{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/labscommunity/intelinside/main/results/schema.json",
  "title": "Intelinside result",
  "description": "One benchmark result, committed as results/<your-github-handle>/<name>.json. Ids come from the hardware, model, quant, and runtime catalog in frontend/src/catalog/. The pull request check validates the ids; this schema covers the shape.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "rig",
    "model",
    "quant",
    "runtime",
    "runtimeVersion",
    "decodeTps",
    "runDate"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "rig": {
      "type": "string",
      "minLength": 1,
      "description": "The number at the end of your rig's URL on the site, or the rig's exact name. The rig must be yours."
    },
    "component": {
      "type": "string",
      "minLength": 1,
      "description": "Hardware id of the one part the run used, for example intel-arc-b580. Leave out for a whole-rig result."
    },
    "componentQuantity": {
      "type": "integer",
      "minimum": 1,
      "description": "How many of that part were used. Needs component."
    },
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "Model id, for example qwen3-8b."
    },
    "quant": {
      "type": "string",
      "minLength": 1,
      "description": "Quant id, for example int4 or q4_k_m. Must be one the model has a board for."
    },
    "runtime": {
      "type": "string",
      "minLength": 1,
      "description": "Runtime id, for example llamacpp, cascadia, openvino-genai."
    },
    "runtimeVersion": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80
    },
    "runtimeFlags": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Flags and settings behind the run: backend, attention kernel, KV cache precision. Optional, for example \"-fa 1 -ngl 99, SYCL backend\"."
    },
    "customRuntime": {
      "type": "string",
      "minLength": 1,
      "description": "The custom runtime this ran on: the number at the end of its URL on the site, or its exact name. Omit for a stock run on the released runtime."
    },
    "revision": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "Custom runtimes only: the exact revision behind this number \u2014 a commit hash, a tag, or a build id. A fork moves week to week, so this is what makes the run reproducible."
    },
    "decodeTps": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Decode tokens per second. This is what ranks."
    },
    "promptTps": {
      "type": "number",
      "exclusiveMinimum": 0
    },
    "ttftMs": {
      "type": "number",
      "exclusiveMinimum": 0,
      "description": "Time to first token, in milliseconds."
    },
    "contextLength": {
      "type": "integer",
      "minimum": 1
    },
    "batchSize": {
      "type": "integer",
      "minimum": 1
    },
    "runDate": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "YYYY-MM-DD"
    },
    "notes": {
      "type": "string",
      "maxLength": 2000
    },
    "evidenceUrl": {
      "type": "string",
      "format": "uri",
      "pattern": "^[Hh][Tt][Tt][Pp][Ss]://\\S+$",
      "description": "Optional HTTPS link to benchmark logs, scripts, a gist, a report, or a repository. The submission PR is recorded separately."
    },
    "result": {
      "type": "string",
      "format": "uri",
      "description": "The result's URL on the site once submitted. Added when the file is written from a result page."
    }
  }
}
